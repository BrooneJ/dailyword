footer    
    nav 
        ul 
            li 
                a(href="/") 
                    i.fas.fa-home
                a(href="/search") 
                    i.fas.fa-search
                a(href="/words/upload")    
                    i.fas.fa-pencil-alt
            if loggedIn               
                li 
                    if loggedInUser.avatarUrl === ""
                        span 😀
                    else 
                        img.AvatarImg(src="/" + loggedInUser.avatarUrl width="50", height="50")
                        div.modal.hidden
                            div.bg
                            div.modalBox
                                a(href=`/users/${loggedInUser._id}`) 
                                    i.fas.fa-edit 
                                    span My word 
                                a(href="/users/edit") 
                                    i.fas.fa-user-edit 
                                    span Edit Profile 
                                a(href="/users/logout") 
                                    i.fas.fa-sign-out-alt 
                                    span Logout 
            else 
                li 
                    a(href="/login")
                        i.fas.fa-user